# Hotel Sales Analysis System

ระบบวิเคราะห์ยอดขายโรงแรม - Siam Kempinski Hotel Bangkok
รวบรวมและสรุปข้อมูลยอดขายรายวันและรายเดือนจาก Itemize Sales Report

## คุณสมบัติ

- 📤 อัพโหลดไฟล์ Excel ยอดขายรายวัน (.xls, .xlsx)
- 📊 สรุปข้อมูลอัตโนมัติ
  - แยกตาม Department (Wine, Liquor, Champagne)
  - แยกตาม Outlet (Sra Bua by Kiin Kiin, ฯลฯ)
  - แยกตาม Check Type (In-House, Walk-In)
- 📅 รายงานรายวันและรายเดือน
- 📈 Top Items ที่ขายดีที่สุด
- 💾 เก็บข้อมูลสะสมทุกวัน
- 📋 สรุปรายเดือนอัตโนมัติ

## การติดตั้ง

1. ติดตั้ง Dependencies:
```bash
npm install
```

2. รันเซิร์ฟเวอร์:
```bash
npm start
```

หรือใช้ Dev mode (auto-reload):
```bash
npm run dev
```

3. เปิดเว็บเบราว์เซอร์ที่:
```
http://localhost:3000
```

## โครงสร้างโปรเจค

```
WebStock/
├── server.js                    # Express server หลัก
├── package.json                 # Dependencies และ scripts
├── utils/
│   ├── hotelExcelReader.js     # อ่านและประมวลผล Excel ยอดขายโรงแรม
│   ├── hotelReportGenerator.js # สร้างรายงาน HTML/Excel
│   └── dataStorage.js          # เก็บข้อมูลรายวันและรายเดือน
├── public/
│   └── hotel.html              # หน้าเว็บหลัก
├── storage/
│   ├── daily_sales.json        # ข้อมูลรายวัน
│   └── monthly_sales.json      # สรุปรายเดือน
├── uploads/                    # โฟลเดอร์เก็บไฟล์อัพโหลด (ชั่วคราว)
└── reports/                    # โฟลเดอร์เก็บรายงาน HTML/Excel
```

## วิธีใช้งาน

### อัพโหลดข้อมูลรายวัน

1. คลิกที่พื้นที่อัพโหลดหรือลากไฟล์ Excel มาวาง
2. กดปุ่ม "อัพโหลดและประมวลผล"
3. ระบบจะ:
   - อ่านวันที่จากแถวที่ 4 ของไฟล์
   - ดึงข้อมูล Name (คอลัมน์ F-G) และ Qty (คอลัมน์ AB)
   - แยกข้อมูลตาม Department, Outlet, Check Type
   - สร้างรายงานรายวันแบบ HTML
   - บันทึกข้อมูลลง storage
   - อัพเดทสรุปรายเดือนอัตโนมัติ

### ดูรายงานรายวัน

- ไปที่แท็บ "รายวัน" เพื่อดูรายการทุกวันที่มีข้อมูล
- คลิก "ดูรายงาน" เพื่อเปิดรายงานรายวันแบบละเอียด

### ดูรายงานรายเดือน

- ไปที่แท็บ "รายเดือน"
- เลือกเดือนที่ต้องการจาก dropdown
- ระบบจะสร้างรายงานสรุปรายเดือนทันที

## รูปแบบข้อมูลในไฟล์ Excel

ไฟล์ Excel ต้องมีรูปแบบดังนี้:

- **แถวที่ 4**: มีวันที่ในรูปแบบ (dd/mm/yyyy)
- **Check Type**: ระบุใน "Check Type: In-House" หรือ "Walk-In"
- **Outlet**: ระบุใน "Outlet: Sra Bua by Kiin Kiin"
- **Department**: ระบุใน "Department: Wine" หรือ "Liquor"
- **Name**: คอลัมน์ F (ชื่อสินค้า)
- **Qty**: คอลัมน์ AB (จำนวนที่ขาย)

## API Endpoints

- `GET /` - หน้าเว็บหลัก
- `POST /api/upload` - อัพโหลดและประมวลผลไฟล์
- `GET /api/stats` - ดูสถิติรวม (จำนวนวัน, เดือน)
- `GET /api/daily/:date` - ดูข้อมูลรายวันของวันที่ระบุ (YYYY-MM-DD)
- `GET /api/monthly/:yearMonth` - สร้างรายงานรายเดือน (YYYY-MM)
- `GET /api/reports` - ดูรายการรายงานทั้งหมด
- `GET /reports/:filename` - ดาวน์โหลดรายงาน

## ข้อมูลที่เก็บ

### รายวัน (daily_sales.json)
```json
{
  "2025-09-24": {
    "date": "2025-09-24",
    "uploadedAt": "2025-09-24T10:30:00.000Z",
    "summary": {
      "totalQty": 150,
      "totalItems": 50,
      "byDepartment": { "Wine": { "qty": 100, "count": 30 } },
      "byOutlet": { "Sra Bua by Kiin Kiin": { "qty": 150, "count": 50 } },
      "topItems": [...]
    },
    "items": [...]
  }
}
```

### รายเดือน (monthly_sales.json)
```json
{
  "2025-09": {
    "month": "2025-09",
    "days": 5,
    "totalQty": 750,
    "totalItems": 250,
    "byDepartment": {...},
    "byOutlet": {...},
    "topItems": {...}
  }
}
```

## เทคโนโลยีที่ใช้

- **Backend**: Node.js + Express
- **Excel Processing**: xlsx (รองรับ .xls และ .xlsx)
- **File Upload**: Multer
- **Data Storage**: JSON files
- **Frontend**: HTML, CSS, JavaScript (Vanilla)

## License

ISC
